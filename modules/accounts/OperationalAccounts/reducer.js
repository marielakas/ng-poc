import defaultState from './model';

const operationalAccounts = (state = defaultState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
};

export default operationalAccounts;